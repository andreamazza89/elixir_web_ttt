defmodule GamePresenterTest do
  use ExUnit.Case

  test "renders a human-v-human new game" do
    #wondering if I should have unit tests for this as the UI is partially tested
    #from the integration tests and likely to be flakey, so don't want to duplicate
    #those...
  end
end
